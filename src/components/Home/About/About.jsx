import { Box } from '@mui/material';
import React from 'react';
import styles from './About.module.css';

const About = () => {
  return (
    <Box 
      id="about-section"
      className={styles.mainAboutSection}>
      About
    </Box>
  );
};

export default About
